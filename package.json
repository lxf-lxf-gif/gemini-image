{
  "name": "seo-writer-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd seo-writer-web && npm install --include=dev && npm run build && cd .. && mkdir -p dist && cp -r seo-writer-web/dist/* dist/"
  },
  "dependencies": {
    "@types/showdown": "^2.0.6",
    "@types/turndown": "^5.0.6",
    "react-quill-new": "^3.7.0",
    "remark-gfm": "^4.0.1",
    "showdown": "^2.1.0",
    "turndown": "^7.2.2"
  }
}
